import { shallowMount } from '@vue/test-utils';
import 'jest';

describe('App component test', () => {
    it('what ', () => {
        expect(1).toBe(1);
        // const wrapper = shallowMount(App, {});
    });
});
